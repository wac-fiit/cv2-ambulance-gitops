apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: oauth2-callback
spec:
  parentRefs:
    - name: wac-hospital-gateway
      sectionName: fqdn
  rules:
# authn callback je presmerovaný na interný envoy proxy
# endpoint 
# musíme ho ale definovať aby ho envoy neodmietol
# a aby sme ho mohli nakonfigurovať bypass security policy
    - matches:
      - path:
          type: Exact
          value: /authn/callback
      filters:
      - type: RequestRedirect
        requestRedirect:
          path: 
            type: ReplaceFullPath
            replaceFullPath: /oauth2/callback
          statusCode: 302