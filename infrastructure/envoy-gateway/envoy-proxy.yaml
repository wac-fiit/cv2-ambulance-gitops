apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: custom-filter-order
  namespace: wac-hospital # Použi namespace, kde máš svoju Gateway
spec:
  provider:
    type: Kubernetes
  filterOrder:
    - name: envoy.filters.http.ext_authz
      after: envoy.filters.http.oauth2